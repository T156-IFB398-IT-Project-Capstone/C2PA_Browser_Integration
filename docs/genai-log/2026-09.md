## 2026-09-07 — feat/sprint2-3-ui-update
Task:            Fresh-clone dev environment setup: diagnose why the local
                 test bench at http://127.0.0.1:8976 was not loading.
Agent produced:  No repo source changes. Ran `node c2pa-test-bench/build.mjs`
                 to generate the gitignored `c2pa-test-bench/verify-bundle.js`.
                 Wrote a throwaway zero-dependency Node static-file server in
                 the session scratchpad (not committed, not added to the repo)
                 to serve `c2pa-test-bench/` on 127.0.0.1:8976.
Human review:    LM confirmed no dependency was added to package.json (esbuild
                 and @contentauth/c2pa-web untouched, c2pa-web still pinned).
                 Decision on whether to add a documented `serve` script to the
                 repo deferred to Lucas.
Verification:    `find . -name package.json -not -path "*/node_modules/*"` ->
                 single root package.json (not a monorepo). No .env/.env.example
                 exists anywhere in the tree; port 8976 is a hardcoded constant
                 in extension/src/shared/constants.js (TEST_BENCH_URLS.local)
                 and extension/src/popup/popup.html, not env-configured.
                 `netstat -ano | findstr 8976` -> no output before start
                 (port free; nothing was conflicting).
                 `node_modules/.bin/esbuild --version` -> 0.25.12; the 25
                 lockfile entries absent from node_modules are all
                 other-platform @esbuild/* optional binaries, so the install
                 is complete for win32-x64 — no reinstall was needed.
                 `node c2pa-test-bench/build.mjs` -> "Done. verify-bundle.js
                 written." (11,124,211 bytes).
                 After starting the server, `netstat -ano | findstr 8976` ->
                 `127.0.0.1:8976 LISTENING 22928`, and curl returned HTTP 200
                 for /, /verify-bundle.js (11.1 MB, text/javascript),
                 /app.js, and /assets/verified-trusted.jpg.
Root cause:      This repo has no dev server and no `dev` script — the root
                 package.json only has `build` and `build:watch`. 8976 is
                 purely a convenience link the popup opens; the test bench is
                 a static page the developer must serve themselves, and its
                 `verify-bundle.js` is gitignored so a fresh clone has to
                 build it first. Nothing was broken.

## 2026-09-07 — feat/sprint2-3-ui-update
Task:            Replace the manual local-dev sequence (npm install → build
                 test bench → start a static server by hand) with a single
                 `npm run dev`, without adding a dependency.
Agent produced:  scripts/dev.mjs (new, ~200 lines): orchestrator that runs the
                 extension watcher as a child process, serves c2pa-test-bench/
                 on 127.0.0.1:8976 from an in-process node:http server, and
                 rebuilds the test-bench bundle only when stale.
                 package.json: added `dev` and `postinstall` scripts.
                 c2pa-test-bench/build.mjs: emit an esbuild metafile
                 (verify-bundle.meta.json) so staleness is computed from the
                 real input list rather than a hardcoded one that would drift.
                 .gitignore: ignore the metafile alongside the bundle.
                 README.md: "Getting started" section (clone → npm install →
                 npm run dev) plus a port-in-use hint.
                 No business logic touched — no file under extension/src/ was
                 modified (the two probe edits during testing were reverted
                 with `git checkout --` and verified clean).
Human review:    LM to review before commit; nothing committed by the agent.
                 Confirmed zero new dependencies (package.json dependencies
                 still only @contentauth/c2pa-web, devDependencies only
                 esbuild; c2pa-web pin untouched). Open question left for LM:
                 postinstall will fail an `npm ci --omit=dev` because esbuild
                 would be absent — acceptable for a dev-only repo, flagged
                 rather than worked around.
Verification:    Freshness metafile: 11 total inputs, 7 outside node_modules
                 (offscreen.js, constants.js, messages.js, badge-map.js,
                 verify-entry.mjs and both trusted-certs PEMs) — so a trust-list
                 edit correctly invalidates the bundle.
                 Skip path: `npm run dev` on an up-to-date tree printed
                 "Test-bench bundle is up to date — skipping rebuild."
                 Stale path: touching extension/src/shared/constants.js's mtime
                 (content unchanged, `git status` clean) made the next run print
                 "missing or stale — building…"; bundle mtime moved 19:00:05 →
                 19:01:31.
                 Extension watcher: appending an output-affecting probe to
                 service-worker.js rebuilt extension/dist/service-worker.js
                 within the same second (19171 → 19227 bytes, probe present in
                 the bundle); reverting the source rebuilt it back to 19171
                 with zero occurrences.
                 Server: HTTP 200 for /, /verify-bundle.js (11,124,211 B),
                 /app.js, /assets/sample-video.mp4 (15,456,823 B, video/mp4);
                 page title "C2PA Verification Test Bench".
                 Path traversal: /%2e%2e%2fpackage.json,
                 /%2e%2e%2f%2e%2e%2fpackage.json and /..%2fpackage.json all
                 return 403; /index.html returns 200.
                 Ctrl-C (requirement: no orphan holding 8976): dev.mjs was
                 started in a real console via Start-Process (PID 15604, child
                 build.mjs --watch 10020, port bound), then a genuine
                 CTRL_C_EVENT was delivered with AttachConsole +
                 GenerateConsoleCtrlEvent (both returned True). Afterwards both
                 PIDs were gone and `netstat -ano | findstr 8976` reported no
                 listener. The watcher is spawned without `shell: true`, so it
                 is a direct child (verified: ParentProcessId 15604) with no
                 cmd.exe wrapper to orphan it.
                 Crash path: killing the watcher PID alone caused the parent to
                 report "Extension watcher exited unexpectedly", shut down and
                 release the port.
                 Port clash: a second concurrent `npm run dev` printed the
                 EADDRINUSE guidance and exited instead of throwing.
                 Fresh clone: deleting verify-bundle.js/.map/.meta.json and
                 running `npm install` alone restored all three via postinstall
                 ("Done. verify-bundle.js written.", 11,124,211 B).

## 2026-09-07 — feat/sprint2-3-ui-update (review only, no merge)
Task:            Review origin/feat/dynamic-c2pa-trust-validation (Jonah) against
                 feat/sprint2-3-ui-update and recommend whether to merge. No
                 merge or commit authorised.
Agent produced:  No repo changes. A disposable worktree + branch
                 (merge-test-scratch) for a trial merge, both since removed, and
                 two throwaway CDP harnesses in the session scratchpad
                 (cdp-probe.mjs, ext-probe.mjs) driving headless Chrome.
Human review:    LM to decide the two design questions below; agent deliberately
                 did not resolve them.
Verification:    Branch: 1 commit, 92b9875 "feat(c2pa): implement dynamic CAWG
                 trust list validation & simplify test bench"; merge-base
                 644eabd. Diffstat: 8 files, +79/-1246 — offscreen.js trust
                 config, c2pa-test-bench/{app.js,index.html}, and deletion of
                 all five trusted-certs/ files (~900 lines of PEM/JSON).
                 Overlap since merge-base = exactly 3 files (app.js,
                 index.html, offscreen.js). constants.js is NOT touched by them.
                 Trial merge (worktree, --no-commit): only app.js conflicts
                 (2 regions); index.html and offscreen.js auto-merge.
                 offscreen.js merged correctly — both branches had independently
                 added the same missing `}` at line 83 (mine in adbdc29), which
                 3-way merge collapsed to one; merged file has no PEM refs and
                 passes a node syntax check.
                 SEMANTIC CONFLICT 1 (git does not catch it): their index.html
                 deletes #modal-json-box while my app.js:379 does
                 getElementById('modal-json-box') and app.js:420 assigns
                 .textContent on it -> TypeError when the inspect modal opens.
                 The auto-merged index.html contains exactly this broken pair.
                 SEMANTIC CONFLICT 2: their app.js replaces real verification
                 with a hardcoded `initialTestCases` array, and its upload
                 handler sets status: VERIFY_STATUS.VERIFIED_TSA, signer
                 'Local Signer', tsaValidated: true for ANY uploaded file — so
                 an unsigned or tampered upload would display "Verified via
                 TSA". Their file header nevertheless reads "Aligned with Real
                 Extension Output & Verification Engine". My branch (9b38137)
                 had replaced that mock with real verify() calls.
                 Empirical trust check (the point of their branch): resolved the
                 merge as recommended (my app.js + index.html, their
                 offscreen.js + trusted-certs deletion), built both bundles
                 clean, and ran the bench in headless Chrome via CDP.
                 MERGED and BASELINE produced byte-identical statuses across all
                 10 reference assets: 3x verified_tsa, 2x verified_untrusted,
                 2x content_tampered, 1x broken_signature, 1x no_credentials,
                 1x unsupported_format. Nothing reads verified_trusted in either
                 configuration — so deleting the local PEM anchors changes
                 nothing observable on this corpus.
                 SDK reading: CawgTrustSettings.verifyTrustList is documented
                 "The default value is 'true'", so their explicit
                 `verifyTrustList: true` is itself a no-op. The SDK's URL
                 resolver only fetches for userAnchors/trustAnchors/trustConfig/
                 allowedList, none of which their config sets — so no new
                 JS-level network call. The WASM does import fetch, so a
                 c2pa-rs-internal CAWG fetch is not ruled out and is UNVERIFIED.
                 Extension check: loaded the merged extension unpacked in
                 headless Chrome; service worker and offscreen targets attached
                 and threw no exceptions; the only page error was a
                 /favicon.ico 404 from the browser itself.
                 Cleanup/no-touch proof: working-tree diff sha256 was
                 d94bf7c2156cc70c7d31cb3d2a3b3554d417370adadf9ed729f44dc05d38dc05
                 before and after; HEAD ae42584 unchanged; merge-test-scratch
                 deleted; `git worktree list` shows only the main tree; no
                 MERGE_HEAD; stash list empty.
Open decisions:  (1) Whether to delete trusted-certs/ at all, given it
                 demonstrably changes no result — and whether CAWG trust-list
                 fetching needs a docs/phase2/decisions/ record under hard
                 constraint #3. (2) Whether the test bench is real-verification
                 (mine) or curated static cases (theirs) — Jonah owns the bench,
                 Lucas owns the verifier.
