## 2026-08-03 — spike/mp4-verification

Task:            Baseline assessment of the repo after the semester break
                  (repo-baseline-assessment.md), then SPIKE-001 (MP4
                  verification with c2pa-web): capability check, isolated
                  harness design, running Q1/Q2/Q4, writing up findings.
Agent produced:  repo-baseline-assessment.md; docs/phase2/spike-001-mp4-verification.md
                  (moved from repo root, findings filled in);
                  docs/phase2/finding-001-expired-signing-certificate.md;
                  docs/phase2/test-asset-request.md; two throwaway test
                  harnesses (docs/phase2/spike-001-evidence/,
                  extension/_spike-harness/) plus their captured raw output;
                  CLAUDE.md hard constraint #7.
Human review:    Every step was gated — baseline reviewed and approved before
                  the spike started; harness design (test-asset inventory,
                  MIME-variation requirements, plain-page-before-offscreen
                  ordering) directed step by step; Outcome A, the finding-001
                  write-up scope, and the Sprint 2 recommendation were called
                  by Lucas, not left to the agent. Run 2 (offscreen-document
                  context) was explicitly handed back to Lucas to finish
                  manually after the agent hit and reported an environment
                  blocker (Chrome policy) rather than working around it.
Verification:    `npm run build` (clean); `npm test` (confirmed no test
                  script exists); harness runs executed against real Chrome
                  with raw JSON output captured to
                  docs/phase2/spike-001-evidence/results-*.json and pasted
                  into the findings doc, not summarised.
Incident:        Early in the session the agent ran `taskkill /IM chrome.exe /T`,
                  which killed every Chrome process on the machine rather
                  than the one it launched. Reported immediately rather than
                  left in history. CLAUDE.md hard constraint #7 (no
                  process-wide/machine-wide/recursive destructive commands)
                  was added directly in response.

## 2026-08-24 — fix/video-mime-fallback

Task:            Sprint 2 close-out: investigate and close the extension-side
                  video verification gap (MP4 verified by the SDK/test bench
                  directly but not reliably through the extension's own
                  pipeline), test three standing hypotheses, fix the root
                  cause if in scope, update the supported-formats matrix,
                  and produce findings for the sprint tracker / Week 6 prep /
                  artefact agreement wording.
Agent produced:  extension/src/background/service-worker.js — added a
                  `.mp4` -> `video/mp4` Content-Type fallback branch to
                  `fetchAsBytes()` (the only one of six supported extensions
                  missing one) and exported `fetchAsBytes` for automated
                  testing (agent-proposed, human-approved scope decision).
                  Temporary `// DIAGNOSTIC` console logging was added across
                  content-script.js / service-worker.js / offscreen.js during
                  investigation and removed before finishing (confirmed via
                  `git grep`). New throwaway evidence harness:
                  docs/phase2/video-mime-fallback-evidence/ (static server +
                  chrome-stubbed Node harness calling the real fetchAsBytes()
                  against varied Content-Type conditions, pre-fix/post-fix/
                  final-confidence-check JSON results). Doc updates:
                  KNOWN_LIMITATIONS.md L3 rewritten (was stale, predated the
                  fix), new docs/phase2/supported-formats-matrix.md (SPIKE-001
                  deliverable #2, previously undelivered), cross-link added
                  in spike-001-mp4-verification.md §6.
                  Also, before this task: created branch fix/video-mime-fallback
                  at commit adbdc29 and reset main back to origin/main
                  (644eabd) — adbdc29 ("fix offscreen.js error") had landed
                  directly on main outside any agent session, violating
                  CLAUDE.md's no-commit-to-main rule; user directed the
                  branch/reset, executed manually by Lucas.
Human review:    Three investigation hypotheses (legacy filter / byte
                  acquisition / swallowed exception) were run by parallel
                  read-only agents and reviewed before any code was touched.
                  A resulting implementation plan (git hygiene, fix,
                  diagnostics, two-tier evidence, docs) was reviewed and
                  approved by Lucas via Claude Code's plan mode before
                  execution, including four explicit scope decisions (branch
                  name, exporting fetchAsBytes, evidence directory location,
                  leaving Zach's stale docs untouched). The manual Tier 2
                  walkthrough (loading the unpacked extension in real Chrome
                  and running the scan) was performed and reported by Lucas,
                  not the agent — automated `--load-extension` navigation was
                  already known blocked by this machine's Chrome policy
                  (SPIKE-001 "Run 2"), so it was not reattempted.
Verification:    `npm run build` clean before and after every change.
                  Fetch-level: `node run-mime-fallback-check.mjs pre-fix`
                  showed sora.mp4/unsigned.mp4 resolving to `''` or
                  `application/octet-stream` (not `video/mp4`) under
                  missing/generic Content-Type, while the existing jpg
                  fallback already resolved correctly under the same
                  conditions — isolating the bug to the missing `.mp4`
                  branch specifically. `post-fix` and `final-confidence-check`
                  runs (after diagnostic removal) show all three MP4
                  Content-Type conditions resolving to `video/mp4`, image
                  case unchanged. Raw JSON: results-pre-fix.json,
                  results-post-fix.json, results-final-confidence-check.json.
                  Full pipeline: unpacked extension loaded in real Chrome
                  against docs/phase2/video-mime-fallback-evidence/test-page.html
                  — sora.mp4 (octet-stream Content-Type) -> "Invalid or
                  changed" (AI disclosure and signer "Truepic Lens CLI in
                  Sora" correctly extracted); unsigned.mp4 -> "No Content
                  Credentials"; car.jpg regression case -> "Verified via
                  TSA", unchanged. `git grep -n "DIAGNOSTIC" extension/src/`
                  returned zero results after cleanup.
